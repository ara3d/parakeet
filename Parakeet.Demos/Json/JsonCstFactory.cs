// DO NOT EDIT: Autogenerated file created on 2023-03-22 2:06:59 PM. 
using System;
using System.Linq;

namespace Parakeet.Demos.Json
{
    public static class CstNodeFactory
    {
        public static CstNode Create(ParserTree node)
        {
            switch (node.Type)
            {
                case "Number": return new Number(node.Contents);
                case "String": return new String(node.Contents);
                case "Constant": return new Constant(node.Contents);
                case "Array": return new Array(node.Contents);
                case "Member": return new Member(node.Children.Select(Create).ToArray());
                case "Object": return new Object(node.Contents);
                case "Element": return new Element(node.Children.Select(Create).ToArray());
                case "Json": return new Json(node.Children.Select(Create).ToArray());
                default: throw new Exception($"Unrecognized parse node {node.Type}");
            }
        }
    }
}
